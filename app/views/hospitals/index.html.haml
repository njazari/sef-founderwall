
-#  This file is app/views/wall/index.html.haml
%h1 Wall of Founders
= form_tag("/", method: "get") do
  %p= label_tag(:q, "Search:") 
  %p= text_field_tag(:q) 
  %p= submit_tag("Search") 

- @hospitals.each do |hospital|
  %p= link_to "#{hospital.name}", hospital_path(hospital)
  %p= "#{hospital.city}, #{hospital.state}"
  %p= "Building Status: #{hospital.building_status}"
  %br
  

  

//   %p= "#{d.hospital}"
//   %p= "#{d.tier}"
//   %p= "#{d.donor.first_name} #{d.donor.last_name}"
//   %p= "#{d.dedication}"
//   %p= "#{d.tier}"
//   %p= "#{d.date}"

= form_for_filterrific @filterrific do |f|
  %div
    Search
    = f.text_field(:search_query,class: 'filterrific-periodically-observed')  
  %div
    Sorted by
    = f.select(:sorted_by, @filterrific.select_options[:sorted_by])
  %div
    = link_to('Reset filters', reset_filterrific_url,)          
  = render_filterrific_spinner

= render(partial: 'dedications/list', locals: { dedications: @dedications })     

%br
%br